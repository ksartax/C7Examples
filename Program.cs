using System;

namespace C7Examples
{
    public partial class Program
    {
        static void Main(string[] args) {
            //new OutExmaple().Tee((el) => { ReadLine(); });

            //new PatternExample().Tee((el) => { ReadLine(); });

            //new ConditionalSwitch().Tee((el) => { ReadLine(); });

            //new Tuples().Tee((el) => { Console.ReadLine(); });
            
            //new Deconstruct().Tee((el) => { Console.ReadLine(); });
            
            //new FunctionInFunction().Tee((el) => { Console.ReadLine(); });


        }
    }
}
